import { Checkbox } from 'antd';
import AntCheckbox from '@ql/styles/checkbox.style';

const checkbox = AntCheckbox(Checkbox);
const CheckboxGroup = Checkbox.Group;

export default checkbox;
export { CheckboxGroup };
